name: CodeQL

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  schedule:
    - cron: "0 3 * * 1"

jobs:
  analyze:
    name: Analyze (typescript)
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    env:
      # prisma.config.ts resolves DATABASE_URL at load time; `prisma generate`
      # (postinstall) needs the variable to exist but never connects to it.
      DATABASE_URL: postgresql://ci:ci@localhost:5432/agentforge

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Set up pnpm
        uses: pnpm/action-setup@v4

      - name: Install foundry deps
        run: pnpm install --frozen-lockfile
        working-directory: foundry

      - name: Install ui deps
        run: npm ci
        working-directory: ui

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: typescript

      - name: Perform CodeQL analysis
        uses: github/codeql-action/analyze@v3
